{
  "name": "exec",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/shared/exec/src",
  "projectType": "library",
  "tags": ["type:shared", "scope:shared"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/shared/exec"
      }
    }
  }
}
