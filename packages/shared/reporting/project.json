{
  "name": "reporting",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/shared/reporting/src",
  "projectType": "library",
  "tags": ["type:shared", "scope:shared"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/shared/reporting"
      }
    }
  }
}
