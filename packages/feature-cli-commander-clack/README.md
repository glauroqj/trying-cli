# Agents CLI — Commander.js + Clack

```bash
pnpm nx run feature-cli-commander-clack:run
# ou
pnpm exec agents-clack
pnpm exec agents-clack start
```

Fluxo: workspace → projetos → skill/hook → global/projeto → instalação com spinner Clack.
