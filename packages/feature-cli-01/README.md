# feature-cli-01

Adapter placeholder para a primeira CLI do benchmark.

## Public API

```ts
import { createCliAdapter } from '@trying-cli/feature-cli-01';

const adapter = createCliAdapter();
```

Substitua `PLACEHOLDER_COMMAND` em `src/domain/constants.ts` quando a CLI real for definida.
