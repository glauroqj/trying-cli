{
  "name": "feature-cli-ink",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/feature-cli-ink/src",
  "projectType": "library",
  "tags": ["type:feature", "scope:feature", "domain:cli-ink"],
  "targets": {
    "run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx packages/feature-cli-ink/src/bin.ts",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/feature-cli-ink/src",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
